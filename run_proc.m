function run_proc(subject_id,version,start_from)
%go through the 10 runs for study, key practice, then the 4 runs in the
%test phase (coded from 1 to 15). Wait for experimenter input between runs.
%If a run returns an error, you can specify where to restart from




end